{
  "name": "luar",
  "version": "0.1.0",
  "description": "Deprecated TypeScript compatibility frontend for Luar editor tooling",
  "deprecated": "Use the Rust luar compiler from luar-rs; this package is retained only for editor compatibility tests.",
  "type": "module",
  "bin": {
    "luar": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
